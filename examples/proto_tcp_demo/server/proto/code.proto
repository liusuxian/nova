syntax = "proto3";

package pb;
option go_package = "./;pb";
option csharp_namespace = "Pb"; // 给 C# 提供的选项

// 响应码
enum Code {
  SUCCESS = 0;       // 成功
  UNKNOWN_CODE = 1;  // 未知错误
  USER_NOTEXIST = 2; // 用户不存在
}
